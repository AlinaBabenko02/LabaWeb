@model AirTickets_project.Plane

@{
    ViewData["Title"] = "Изменить";
}

<h1>Изменить</h1>

<h4>Самолет</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PlanesId" />
            <div class="form-group">
                <label asp-for="ModelsId" class="control-label"></label>
                <select asp-for="ModelsId" class="form-control" asp-items="ViewBag.ModelsId"></select>
                <span asp-validation-for="ModelsId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Сохранить" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Назад</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
